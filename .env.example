# ============================================================
# Bíblia Alpha — Variáveis de Ambiente
# ============================================================
# Copie para .env.local e preencha. NUNCA commite .env.local

# Gemini (tradução de comentários)
GEMINI_API_KEY=

# API.Bible (traduções adicionais)
VITE_BIBLE_API_KEY=

# ── Mercado Pago (configurar via Firebase Secrets, não aqui) ──
# Use: firebase functions:secrets:set MP_ACCESS_TOKEN
# Use: firebase functions:secrets:set MP_WEBHOOK_SECRET
# Use: firebase functions:secrets:set MP_PUBLIC_KEY
#
# MP_ACCESS_TOKEN=   ← NUNCA coloque aqui
# MP_WEBHOOK_SECRET= ← NUNCA coloque aqui
# MP_PUBLIC_KEY=     ← NUNCA coloque aqui
